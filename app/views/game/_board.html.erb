<table>
  <% [(0..2), (3..5), (6..8)].each do |row| %>
    <tr>
      <% row.each do |index| %>

        <td data-name="cell" data-id="<%= index %>">
          <% if board[index] %>
            <%= board[index] == 'X' ? translate(:X) : translate(:O) %>
          <% else %>
            <%= link_to "", move_path(board: board, ai_type: ai_type, move: index) %>
          <% end %> 
        </td>

      <% end %>
    </tr>
  <% end %>
</table>
